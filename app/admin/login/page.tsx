"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { supabase } from "@/lib/subabase/client";
export default function AdminLogin() { const [email, setEmail] = useState(""), [password, setPassword] = useState(""), [message, setMessage] = useState(""), [sending, setSending] = useState(false); const submit = async (e: FormEvent) => { e.preventDefault(); setMessage(""); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) return setMessage("Invalid email or password."); window.location.href = "/dashboard"; }; const reset = async () => { if (!email) return setMessage("Enter your admin email first, then choose reset password."); setSending(true); const { error } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/admin/reset-password` }); setSending(false); setMessage(error ? error.message : "Password-reset email sent. Check your inbox and spam folder."); }; return <main className="flex min-h-screen items-center justify-center bg-[#050505] p-6 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.04] p-8"><p className="text-cyan-300">NEXUS ADMIN</p><h1 className="mt-3 text-3xl font-bold">Sign in</h1><input value={email} onChange={(e) => setEmail(e.target.value)} type="email" placeholder="Admin email" className="mt-7 w-full rounded-xl bg-black/30 p-3" required /><input value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="Password" className="mt-3 w-full rounded-xl bg-black/30 p-3" required /><button className="mt-5 w-full rounded-xl bg-white p-3 font-semibold text-black">Sign in</button><button type="button" onClick={reset} disabled={sending} className="mt-4 w-full text-sm text-cyan-300 hover:text-white disabled:opacity-50">{sending ? "Sending…" : "Forgot password?"}</button><p className="mt-4 text-sm text-gray-300">{message}</p><Link href="/" className="mt-6 block text-center text-sm text-gray-400 hover:text-white">Back to site</Link></form></main>; }
