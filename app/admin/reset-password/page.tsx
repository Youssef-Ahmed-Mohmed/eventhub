"use client";
import { FormEvent, useState } from "react";
import { supabase } from "@/lib/subabase/client";
export default function ResetPassword() { const [password, setPassword] = useState(""), [message, setMessage] = useState(""); const submit = async (e: FormEvent) => { e.preventDefault(); if (password.length < 8) return setMessage("Use at least 8 characters."); const { error } = await supabase.auth.updateUser({ password }); if (error) return setMessage(error.message); setMessage("Password updated. Redirecting to dashboard…"); setTimeout(() => { window.location.href = "/dashboard"; }, 900); }; return <main className="flex min-h-screen items-center justify-center bg-[#050505] p-6 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.04] p-8"><p className="text-cyan-300">NEXUS ADMIN</p><h1 className="mt-3 text-3xl font-bold">Set a new password</h1><input autoFocus value={password} onChange={(e) => setPassword(e.target.value)} type="password" minLength={8} placeholder="New password (8+ characters)" className="mt-7 w-full rounded-xl bg-black/30 p-3" required /><button className="mt-5 w-full rounded-xl bg-white p-3 font-semibold text-black">Update password</button><p className="mt-4 text-sm text-gray-300">{message}</p></form></main>; }
